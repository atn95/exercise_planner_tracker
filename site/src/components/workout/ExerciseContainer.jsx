const ExerciseContainer = (props) => {
	let styles = {
		container: {
			display: `flex`,
			flexDirection: `column`,
		},
	};
	return <div style={styles.container}>ExerciseContainer</div>;
};

export default ExerciseContainer;
